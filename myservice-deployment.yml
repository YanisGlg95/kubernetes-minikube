apiVersion: apps/v1
kind: Deployment
metadata:
  name: myservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myservice
  template:
    metadata:
      labels:
        app: myservice
    spec:
      containers:
        - image: yanisglg95/serviceyanis:1
          imagePullPolicy: IfNotPresent
          name: serviceyanis
      restartPolicy: Always
